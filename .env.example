# Copy to .env (already provided with dev defaults).
# For production swap to PostgreSQL per SoW and set a strong JWT secret.

# SQLite for out-of-the-box run:
DATABASE_URL="file:./dev.db"
# PostgreSQL example:
# DATABASE_URL="postgresql://user:pass@localhost:5432/sales_dashboard"

JWT_SECRET="dev-secret-change-me-in-production-please-32chars"
